name: Verify production BTC macro 4 of 4 c9de2b3

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify-production-btc-macro:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      PRODUCTION_RADAR_API_BASE_URL: ${{ vars.PRODUCTION_RADAR_API_BASE_URL }}
      PRODUCTION_RADAR_API_KEY: ${{ secrets.PRODUCTION_RADAR_API_KEY }}
      EXPECTED_SHA: c9de2b3978aca83d1343ca4062f6d7b4181f14b5
    steps:
      - name: Check out exact production commit
        uses: actions/checkout@v7
        with:
          ref: c9de2b3978aca83d1343ca4062f6d7b4181f14b5
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Verify BTC macro cycle ETF production layer with 4 of 4 macro coverage
        working-directory: bybit_eu_swing_radar/backend
        run: python scripts/production_btc_macro_cycle_etf_shadow_smoke.py
