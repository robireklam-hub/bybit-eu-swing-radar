name: Diagnose Railway scope for recorder provisioning b54fbd0

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  diagnose:
    if: github.head_ref == 'ops/provision-microstructure-recorder-b54fbd0'
    runs-on: ubuntu-latest
    container: ghcr.io/railwayapp/cli:latest
    timeout-minutes: 5
    env:
      RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_API_TOKEN }}
      PROJECT_ID: 846f5088-4e8b-45fe-84d9-0728542b2c36
      ENVIRONMENT_ID: 2be04d3c-82b3-487d-930c-a4fbc7d483d8
    steps:
      - name: Verify account/workspace token presence
        run: |
          if [ -z "${RAILWAY_API_TOKEN:-}" ]; then
            echo "FAIL RAILWAY_API_TOKEN is not configured"
            exit 1
          fi
          echo "RAILWAY_API_TOKEN present; value redacted."

      - name: Inspect authenticated identity and visible workspaces
        run: railway whoami --json

      - name: Inspect visible projects and workspace metadata
        run: railway list --json

      - name: Probe project link without mutation
        run: railway link --project "$PROJECT_ID" --environment "$ENVIRONMENT_ID" --json
